## 1. Verify tintColor responds to a conditional battery expression

- [x] 1.1 On the ball `PartImage` only, add `Transform target="tintColor" value="[BATTERY_PERCENT] < 100 ? '#FF2222' : '#FFE600'"` (loosened threshold to force the warning state visibly for testing; exact string/color-literal syntax to confirm against the arithmetic-expression reference). **Outcome:** hex-string ternary syntax is wrong for `tintColor` — colors must be constructed with `colorArgb(a,r,g,b)`/`colorRgb(r,g,b)`, confirmed via the official effects/expressions docs (same rule as `ColorConfiguration` values). Applied to the Classic-layout ball only: `value="[BATTERY_PERCENT] &lt; 100 ? colorArgb(255,255,34,34) : colorArgb(255,255,230,0)"` (`&lt;` since a literal `<` isn't valid inside an XML attribute).
- [x] 1.2 Build, install, check `adb logcat` for parse/evaluation failures. **Outcome:** clean build, clean logcat, no errors — `colorArgb()` inside a ternary on `tintColor` is valid syntax.
- [x] 1.3 Screenshot to confirm the ball renders red (forced warning state). **Outcome:** confirmed. Note: the emulator's default battery level is 100%, so the `< 100` threshold never actually triggers at rest — used `adb shell dumpsys battery set level 15` to force a real low-battery state for testing, which is a more faithful test of the real `< 20` threshold anyway.
- [x] 1.4 Change the threshold back toward a real value (`< 20`) and confirm the ball returns to its normal yellow color at the current (presumably higher) battery level. **Outcome:** confirmed — with `adb shell dumpsys battery set level 85`, ball renders its normal yellow.

## 2. Apply to both paddles

- [x] 2.1 Apply the same pattern (with the real `< 20` threshold) to both paddle `PartImage`s. **Outcome (revised per live user feedback):** initially applied to both Classic and Scoreboard paddles too (5 elements beyond the Classic ball), but the user asked to keep the ball-only tint — paddle tinting was reverted. Final scope: only the ball tints (in both Classic and Scoreboard layouts, since the arena appears in both); paddles always stay their normal white.
- [x] 2.2 Build, install, check logcat, screenshot to confirm all three elements (ball + 2 paddles) tint together. **Outcome (revised):** superseded by the ball-only scope decision — verified instead that only the ball turns red at low battery (both layouts) while both paddles stay white, via `adb shell dumpsys battery set level 15`. Clean build, clean logcat throughout.

## 3. Final verification

- [x] 3.1 Confirm normal (non-low-battery) rendering is unaffected across a couple of the 15 color themes. **Outcome:** checked Night Vision and War Room at normal battery — background colors change per theme as expected, ball/paddles stay their normal white/yellow (the battery tint and theme system are fully independent).
- [x] 3.2 Confirm ambient mode still works correctly with the new expression in place. **Outcome:** confirmed at 15% battery — the ball still renders the red warning tint in ambient mode, dimmed via the existing `Variant` alpha like everything else. No regression.
- [x] 3.3 Confirm no build/dependency changes were introduced — module stays `hasCode = false`. **Outcome:** confirmed — only `watchface/src/main/res/raw/watchface.xml` changed; no build/manifest changes needed.
- [x] 3.4 Update this file's checkboxes with outcomes, including the confirmed expression syntax. **Outcome:** confirmed syntax: `Transform target="tintColor" value="[BATTERY_PERCENT] &lt; 20 ? colorArgb(255,255,34,34) : colorArgb(255,255,230,0)"` on the ball `PartImage` (both Classic and Scoreboard layout instances). Hex-string literals (`'#FF2222'`) don't work for `tintColor` — must use `colorArgb()`/`colorRgb()`, matching the existing rule for `ColorConfiguration` values. Final scope is ball-only, not paddles, per explicit user direction during implementation.
